@{
  Layout = "_Layout";
}

@using VOT.Models;

<h1>Vendors</h1>

@if (Model.Count == 0)
{
  <h4>No Current Vendors</h4>
}

@foreach(Vendor vendor in Model)
{
  <h4><a href='/vendor/@vendor.Id'>@vendor.Name</a></h4>
}

<a href="/vendor/new">Create a new vendor</a>